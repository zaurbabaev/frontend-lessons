/* const paragraphs = document.getElementsByTagName("p");

for (p of paragraphs) {
    console.log(p.innerText);

} */

const elemets = document.querySelectorAll(".text");
for (elem of elemets) {
    console.log(elem.innerText);

}